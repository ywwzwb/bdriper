services:
  bdriper:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./data:/data
      - /mnt/bdmv:/input:ro
      - /mnt/output:/output
    devices:
      - /dev/dri:/dev/dri
    environment:
      - MAX_CONCURRENT=2
    restart: unless-stopped

  # GPU variant (uncomment if NVIDIA GPU available):
  # NOTE: `video` capability is required for NVENC/NVDEC. Without it the
  # nvidia-container-toolkit only injects `utility,compute` libraries and
  # ffmpeg fails with "Cannot load libnvidia-encode.so.1".
  # bdriper-gpu:
  #   build: .
  #   ports:
  #     - "8080:8080"
  #   volumes:
  #     - ./data:/data
  #     - /mnt/bdmv:/input:ro
  #     - /mnt/output:/output
  #   deploy:
  #     resources:
  #       reservations:
  #         devices:
  #           - driver: nvidia
  #             count: 1
  #             capabilities: [gpu, compute, utility, video]
  #   restart: unless-stopped
