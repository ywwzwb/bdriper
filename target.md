编写一个容器镜像(docker), 这个容器提供一个可视化界面(web server), 用来压制bdmv 文件为bdrip(mkv)
由于视频文件都很巨大, 所以源文件与目标都使用容器内挂在的文件.
分为任务管理, 设置三个tab
* 概览: 
  * 显示CPU 占用率, GPU 占用率, 当前是否有任务. 中间显示一个新增任务按钮.
* 任务管理:
  * 任务列表, 可以看到当前进行中, 已完成的任务
    * 任务列表中,可以看到任务的名称, 进度, 预估完成时间
    * 可以按照状态筛选(失败/进行中/已完成/等待执行).
    * 进行中可以删除, 暂停. 删除后, 任务自动取消.
    * 失败的任务可以重试, 删除.
    * 其他任务只能删除
    * 有一个一键删除已完成任务.
    * 已经完成的任务, 删除时, 不会删除转码的文件. 其他的(进行中, 失败等, 未开始) 都应删除文件(若有)
    * 任务可以多选后删除/暂停(如果选中的都是进行中的任务)
    * 任务详细, 点击任务后, 自动展开任务详细, 再次点击, 则关闭详细信息.
      * 详细信息中, 除了基本的信息之外, 包括任务的源文件路径, 目标路径, 转码配置(链接), pid(如果有)
  * 新增任务, 新增任务是一个多步骤的引导界面
    1. 源文件选择: 用文件框的模式, 需要选择一个源文件路径(容器内). 支持选择bdmv 文件夹, bdmv镜像(iso).
    2. 选中后, 用户将进入下一步, 开始自动解析源文件信息, 如果解析失败, 则报错, 用户可以点击推出结束任务新建流程.
    3. 源文件解析成功后, 列出主文件列表, 以及附属文件列表(如菜单等). 用户可以选中一个或多个文件, 每一个列表, 提供一个全选功能. 默认主文件全部选中.
    4. 文件列表中, 可以选择需要附加的字幕, 音轨, 章节等信息, 每一个都是一个下拉的多选框, 默认全部选中(所有的音轨, 所有的字幕, 启用章节信息)
    5. 用户选择一个或多个文件后, 点击下一步进入转码配置界面
    6. 用户可以选择一个已有的配置, 也可以选择新建一个配置.
    7. 选择配置之后, 点击下一步选择目标位置, 这个界面, 有一个转码预览按钮, 点击预览后, 用户可以选择任意一个任务列表中的文件, 设置一个开始时间戳以及长度, 点击开始转码, 转码保存到临时目录, 完成后, 提供一个下载地址, 用户可以下载这个文件. 预览任务可以取消, 也会自动放到任务列表中, 但是执行结束后, 自动删除, 删除后, 临时文件只保存4个小时. 用户觉得预览没问后, 关闭转码预览界面, 临时文件也会自动删除. 
    8. 设置好目标路径, 点击完成, 则开始创建任务开始转码.
* 设置界面
  * 系统设置:
    * 最多同时任务数量
    * 启用GPU 加速, 旁边应该有一个按钮, 用来测试当前GPU 支持哪些编解码
  * 转码配置管理.
    * 列举现有的转码配置
      * 点击后, 显示详细参数
    * 新增配置
      * 关于转码的配置应该如何设计, 你应该充分研究 downloads\markdown 中的资料.
      * 新增配置, 可以选择专业模式和简易模式
        * 专业模式下, 大部分可用选项
        * 简易模式下, 只提供诸如编码器选择, 质量低中高等基本选项.
* 后端: 使用golang + ffmpeg 等实现.